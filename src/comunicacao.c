// autor: Jardel Carvalho
// ano: 2019

#include "comunicacao.h"

int *dependencias(int rank) {
    int *p_ranks;
    int g;

    //... g? 
    //...

    p_ranks = malloc(g * sizeof(int));
    if(!p_ranks) {
        printf("erro: problema ao alocar vetor de vizinhos\n");
        return NULL;
    }

    //... p_ranks?
    //...


    return p_ranks;
}